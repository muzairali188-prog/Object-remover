
{
  "name": "object-remover-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Professional AI Image Object Removal Suite",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build step required for ESM-native app' && mkdir -p dist && cp -r * dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "@google/genai": "^1.34.0"
  }
}
